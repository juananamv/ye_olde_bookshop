<!DOCTYPE html>
<html>
  <head>
    <title>YeOldeBookshop</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.1/css/bulma.min.css">
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>


<header class="container">
  <nav class="navbar" role="navigation">
    <div class="navbar-brand">
      <a class="navbar-item" href="/products">
        <img src="https://bulma.io/images/bulma-logo.png" alt="Bulma: Free, open source, and modern CSS framework based on Flexbox" width="112" height="28">
      </a>
    </div>
    <div class="navbar-menu">
      <div class="navbar-start">
        <a class="navbar-item" href=<%= products_path %>>
          All Products
        </a>
        <a class="navbar-item" href=<%= categories_path %>>
          Categories
        </a>
      </div>
    </div>
    <form action="/products" method="get">
    <div class="navbar-end">
      <div class="select navbar-item mt-2 ml-3">
        <select id="category" name="category">
          <option value="">All Products</option>
          <% Category.all.each do |category| %>
            <option value="<%= category.id%>"><%= category.name %></option>
          <% end %>
        </select>
      </div>
        <input class="input navbar-item mt-2" type="text" id="query" name="query" placeholder="Search Products..."/>
        <input class="button is-primary mt-2 ml-3" type="submit" value="Search">
    </div>
    </form>
  </nav>
</header>







    <div class="container is-align-items-center is-justify-content-center">
      <%= yield %>
    </div>
  </body>
</html>
